import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App';

// No need to import reportWebVitals if you're not using it

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);

// If you find reportWebVitals here, comment or remove it
// reportWebVitals();
